<Window x:Class="MoodleLogAnalyse.ChartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ChartWindow" Height="368.8" Width="1100">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />

            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <ScrollViewer Grid.Column="1" HorizontalScrollBarVisibility="Visible" Margin="0,0,-0.4,0.4">
            <Canvas x:Name="ChartCanvas" Height="800" Width="800" Background="Beige"/>
        </ScrollViewer>
        <Button Grid.Column="0" Content="Display Chart" HorizontalAlignment="Left" Margin="8,10,0,0" VerticalAlignment="Top" Click="Button_Click" Width="182"/>
        <ListBox x:Name="ModuleSelector" HorizontalContentAlignment="Stretch" HorizontalAlignment="Left" Height="295" Margin="8,34,0,0" VerticalAlignment="Top" Width="182" SelectionMode="Multiple">
        <ListBox.ItemTemplate>
            <DataTemplate>
                <Grid Margin="0,2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="20" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding id}" />
                        <TextBlock Grid.Column="1" Text="{Binding name}" />
                    </Grid>
            </DataTemplate>
        </ListBox.ItemTemplate>
        </ListBox>

    </Grid>
</Window>
